"""WP Publisher — a site-agnostic WordPress publishing engine.

Turn a document into a fully formatted, SEO-optimized, schema-rich WordPress
page driven by per-page-type templates. All site identity (organization, URL,
SEO defaults, templates) lives in configuration, so the same engine powers any
number of sites — one repository per site.
"""

__version__ = "0.1.0"
