# Dashboard (Streamlit) + the wp_publisher engine's runtime deps.
# The engine is imported from ../src via sys.path (no editable install needed),
# so we only list third-party libraries here.
streamlit>=1.36
requests>=2.31
python-docx>=1.1
markdown>=3.6
beautifulsoup4>=4.12
lxml>=5.0
PyYAML>=6.0
python-frontmatter>=1.1
pydantic>=2.6
